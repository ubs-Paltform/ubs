{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the UBS build window",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "dialog:allow-open"
  ]
}
