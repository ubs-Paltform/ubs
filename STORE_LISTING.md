# 스토어 등록 카피 (다국어)

App Store / Google Play 공용 등록 카피. 앱 이름 `UBS`는 번역하지 않는다.
외부 서비스·프레임워크·상표명 없이 UBS 자체 기능을 중심으로 작성했다.

---

## 한국어 (ko)

### 제목

```
UBS - 빌드 자동화
```

### 부제

```
프로젝트 감지부터 안전한 배포까지
```

### 간단한 설명

```
프로젝트를 자동으로 찾아 의존성 순서에 맞춰 빌드하고 안전하게 배포하세요.
```

### 프로모션 텍스트

```
여러 프로젝트의 빌드와 배포를 한 흐름으로 관리하세요. 자동 감지, 병렬 처리, 사전 점검, 서명된 업데이트와 롤백을 지원합니다.
```

### 자세한 설명

```
UBS는 여러 프로젝트로 구성된 작업 공간의 빌드와 배포를 한 곳에서 관리하는 빌드 자동화 도구입니다.

■ 프로젝트 자동 감지
설정 파일을 일일이 작성하지 않아도 작업 공간을 살펴 빌드 가능한 프로젝트를 찾고 유형을 분류합니다.

■ 의존성 순서에 맞는 빌드
프로젝트 사이의 의존성을 분석해 올바른 순서로 빌드합니다. 독립적인 작업은 병렬로 처리하고, 필요한 경우 순차 실행으로 전환할 수 있습니다.

■ 실행 전 계획과 점검
실제 실행 전에 빌드 계획을 확인할 수 있습니다. 감사 결과와 의존성 그래프를 구조화된 형식으로 출력해 자동화 파이프라인에도 연결할 수 있습니다.

■ 터미널과 자동화 환경 지원
대화형 명령뿐 아니라 입력 없이 실행하는 모드도 제공해 반복 작업과 배포 파이프라인에 활용할 수 있습니다.

■ 안전한 업데이트
서명된 업데이트를 적용하고 문제가 생기면 이전 상태로 되돌릴 수 있도록 설계했습니다.

■ 하나의 흐름으로 관리
감지, 계획, 빌드, 점검, 배포 과정을 하나의 명령 체계로 연결해 반복 작업을 줄입니다.

작업 공간의 복잡한 빌드 과정을 더 예측 가능하고 관리하기 쉬운 흐름으로 바꿔보세요.
```

### 키워드

```
빌드자동화,모노레포,CI/CD,릴리스관리,의존성관리,병렬빌드,안전배포,업데이트,롤백,개발도구
```

---

## English (en)

### 제목

```
UBS - Build Automation
```

### 부제

```
Detect, build, release
```

### 간단한 설명

```
Find projects, build in dependency order, and release with confidence.
```

### 프로모션 텍스트

```
Bring project detection, dependency-aware builds, checks, signed updates, and rollback into one reliable workflow.
```

### 자세한 설명

```
UBS is a build automation tool for managing workspaces made of multiple projects.

■ Automatic project detection
Scan a workspace, find buildable projects, and classify them without requiring a separate configuration file for every project.

■ Dependency-aware builds
Analyze project dependencies and build in the correct order. Independent tasks can run in parallel, while sequential execution remains available when you need it.

■ Plans and checks before execution
Preview the build plan before starting. Export audit results and dependency graphs in a structured format for automation pipelines.

■ Terminal and automation workflows
Use interactive commands when you want guidance, or run without prompts for repeatable builds and release pipelines.

■ Safer updates
Apply signed updates and keep a path back to the previous state when a problem occurs.

■ One connected workflow
Link detection, planning, building, auditing, and release steps through one command system to reduce repetitive work.

Turn complex workspace builds into a workflow that is easier to understand, repeat, and control.
```

### 키워드

```
build automation,monorepo,CI/CD,release management,dependency graph,parallel build,safe deployment,rollback,developer tools
```

---

## 日本語 (ja)

### 제목

```
UBS - ビルド自動化
```

### 부제

```
検出から安全な配布まで
```

### 간단한 설명

```
プロジェクトを検出し、依存関係に沿ってビルドと配布を自動化します。
```

### 프로모션 텍스트

```
プロジェクト検出、依存関係に沿ったビルド、事前確認、署名付き更新、ロールバックを一つの流れで管理できます。
```

### 자세한 설명

```
UBSは複数のプロジェクトで構成されたワークスペースのビルドと配布を管理する自動化ツールです。

■ プロジェクトを自動検出
ワークスペースを調べてビルド可能なプロジェクトを見つけ、個別の設定ファイルを増やさずに種類を分類します。

■ 依存関係に沿ったビルド
プロジェクト間の依存関係を分析し、正しい順番でビルドします。独立した処理は並列で実行でき、必要に応じて順番に実行することもできます。

■ 実行前の計画と確認
開始前にビルド計画を確認できます。監査結果と依存関係グラフを構造化された形式で出力し、自動化パイプラインに接続できます。

■ ターミナルと自動化環境に対応
案内付きの対話的なコマンドに加え、入力なしで実行するモードも利用できます。繰り返しのビルドや配布に便利です。

■ 安全な更新
署名付きの更新を適用し、問題が発生した場合は以前の状態へ戻せるように設計されています。

■ 一つの流れで管理
検出、計画、ビルド、確認、配布を一つのコマンド体系でつなぎ、繰り返し作業を減らします。

複雑なワークスペースのビルドを、理解しやすく再現可能な流れに変えましょう。
```

### 키워드

```
ビルド自動化,モノレポ,CI/CD,リリース管理,依存関係,並列ビルド,安全な配布,更新,ロールバック,開発ツール
```

---

## 简体中文 (zh)

### 제목

```
UBS - 构建自动化
```

### 부제

```
从检测到安全发布
```

### 간단한 설명

```
自动发现项目，按依赖顺序构建，并更安心地完成发布。
```

### 프로모션 텍스트

```
将项目检测、依赖构建、发布前检查、签名更新和回滚整合到一个可靠的工作流中。
```

### 자세한 설명

```
UBS 是一款用于管理多项目工作区构建与发布的自动化工具。

■ 自动发现项目
扫描工作区，查找可构建项目并识别项目类型，无需为每个项目单独维护配置文件。

■ 按依赖关系构建
分析项目之间的依赖关系，按正确顺序执行构建。相互独立的任务可以并行运行，也可以按需改为顺序执行。

■ 执行前查看计划
开始前先查看构建计划。审计结果和依赖关系图可以结构化格式输出，方便接入自动化流水线。

■ 支持终端和自动化流程
需要引导时使用交互式命令，也可以在无需输入的模式下运行，适合重复构建和发布流程。

■ 更安全的更新
应用签名更新，并在出现问题时保留回退到之前状态的路径。

■ 统一工作流
通过一套命令体系连接检测、计划、构建、审计和发布，减少重复操作。

让复杂工作区的构建变得更清晰、更容易重复和控制。
```

### 키워드

```
构建自动化,单体仓库,CI/CD,发布管理,依赖关系,并行构建,安全发布,更新,回滚,开发工具
```
