[package]
name = "ubs-helper"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Optional native integrity helper for Universal Build Script"

[dependencies]
sha2 = "0.11"

[profile.release]
lto = "thin"
strip = "symbols"
codegen-units = 1
panic = "abort"
